# T-Pot Honeypot Analyzer Configuration
# Copy this file to .env and customize for your setup

# =============================================================================
# Elasticsearch Configuration (T-Pot Hive)
# =============================================================================
ES_HOST=localhost
ES_PORT=64298
ES_INDEX_PATTERN=logstash-*
ES_USE_SSL=false
ES_VERIFY_CERTS=true
# Uncomment if your ES requires authentication:
# ES_USERNAME=elastic
# ES_PASSWORD=your_password

# =============================================================================
# LLM Configuration (LM Studio or OpenAI-compatible API)
# =============================================================================
LLM_HOST=localhost
LLM_PORT=1234
# Optional: Override the full API URL
# LLM_API_URL=http://localhost:1234/v1/chat/completions
# Optional: Specify a model (empty = use default loaded model)
# LLM_MODEL=mistral-7b
# Optional: API key for OpenAI/Anthropic/other providers
# LLM_API_KEY=sk-...

# =============================================================================
# Analysis Settings
# =============================================================================
REQUEST_TIMEOUT=120
DELAY_BETWEEN_REQUESTS=0.5
MAX_CONSECUTIVE_FAILURES=3

# =============================================================================
# Honeypot Filtering
# =============================================================================
# Comma-separated list of honeypot types to exclude
EXCLUDE_TYPES=Fatt,Suricata,P0f

# =============================================================================
# Output Settings
# =============================================================================
OUTPUT_DIR=output
CACHE_FILE=analysis_cache.json
